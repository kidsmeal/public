# <Feature> - Design

Status: draft | reviewed | approved
Intent: <one sentence - what this feature is for, in a party/user/system sense.>

## Problem
<what is missing or broken today, and why it matters. No solution yet.>

## Design
<the proposed shape. Break it into the design's natural unit (component / screen / endpoint
/ rule / entry). For each unit, give enough concrete detail - data shape, behavior,
observable outcome - that a planner could decompose it without re-asking you.>

## Contracts touched
<shared state, schemas, public APIs, events, generated code, build/release config this
feature changes. For each: the change and the compatibility/migration story. If none, say
"none".>

## Edge cases
<empty / zero / max / error / offline / concurrent states that matter here.>

## Out of scope
<things a reader might assume are included but are not. Pins the boundary.>

## Open questions
<unmade decisions. The design-reviewer turns anything that quietly assumes one of these
into a [NEEDS USER DECISION] marker, so list them honestly.>
