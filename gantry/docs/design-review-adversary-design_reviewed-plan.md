# Design-review adversary activation (v2) - Implementation Plan

Source design: `docs/design-review-adversary-design_reviewed.md` (status: reviewed; the one decision that deferred this to v2 - the fix-loop routing - is resolved in the design's Open questions section: the adversary's findings fold into Stage 1's existing resolve-and-re-run loop, capped under the same informed-escalation rule as Stage 3. No open question blocks planning.)
Conventions read: no `CONVENTIONS.md`/`CLAUDE.md` exists in this repo. De facto house style confirmed by reading `plugins/gantry/scripts/role.js`, `plugins/gantry/skills/gantry/SKILL.md`, `plugins/gantry/commands/{design,models,run}.md`, `test/consistency.test.js`, `test/role.test.js`, and the gold-standard prior plan `docs/review-gate-hardening-design_reviewed-plan.md` (which this plan mirrors). House style: plain Node.js, `"use strict"`, zero runtime deps, a top-of-file block comment, terse, fail-safe. Agent/skill/command prose carries no AI-tells (no em-dashes, no "not X but Y", no three-beat lists). The added prose matches the existing prose in each file touched.
Verification command(s): `node --test` from the repo root `C:\Users\atk67\Documents\Github\gantry` (package.json `scripts.test` is `node --test`; tests live in repo-root `test/`). Baseline at planning time: **174 pass, 0 fail.** Per-phase verification below names the specific test file(s) to add/extend and run.

## Summary
This activates the already-resolved-but-dormant `design-reviewer` adversary so the design gate gets the same optional cross-model second opinion the phase gate already has. Four phases, all additive: a project with no `design-reviewer` adversary configured sees byte-for-byte today's design gate. One code edit (a role-appropriate prompt note in `role.js`), three prose edits (`SKILL.md` Stage 1, `design.md`, `run.md`), the `models.md` caveat drop, and the test artifacts - including the **required flip** of the existing `consistency.test.js` assertion that pins `models.md` as "dormant / reserved for v2", which must land in the same phase as the `models.md` edit or the suite goes red.

## Blockers / Open Questions
None block Phase 1. The design's Open questions section records the one deferred decision (fix-loop routing) as resolved by the human gate this session and baked into Unit 1. The spot-check confirmed every file and line the design names exists and is as described:
- `SKILL.md` Stage 1 is the design-review gate (lines 27-32); the resolve-and-re-run loop the new step folds into is steps 2-3; Stage 3 step 5 (lines 48-51) is the existing `phase-reviewer` adversary to mirror.
- `role.js` `cmdRun` has `role` (line 159), `adversaryMode` (line 164), and the diff-worded prompt note (lines 217-222) all in scope; the existing `role === "implementer"` branch (line 240) confirms branching on `role` inside `cmdRun` is already the established pattern.
- `design.md` runs the `design-reviewer` standalone with the `DISPATCH: external` branch that writes `<draft>_reviewed.md` itself (line 11).
- `models.md` line 29 carries the "reserved for v2" / "does not fire in the orchestrator yet" caveat to drop.
- `run.md` item 1 names the design-review gate but no design adversary (line 12); item 3 already spells out the `phase-reviewer` adversary, the pattern to mirror (line 14).
- `consistency.test.js:402-414` is the existing assertion that matches `models.md` against `/dormant|v2|reserved/i` and **must be flipped** in the same phase as the `models.md` edit.
- `role.test.js` has the `mk()` / `writeConfig()` / `resolve()` / `show()` / `runAdversary()` helpers (lines 14-38) and the `"show: dormant design-reviewer adversary is parsed and shown"` test (lines 235-260) whose comment text ("dormant (v2)") becomes stale.

No convention violation is implied by the design. Nothing prevents starting Phase 1.

## Layout facts the implementer must respect (confirmed by spot-check)
- The orchestrator is `plugins/gantry/skills/gantry/SKILL.md`; the command surfaces are `plugins/gantry/commands/{design,run,models}.md`. The one code file is `plugins/gantry/scripts/role.js`.
- `role-core.js` and the `resolve` / `run --adversary` / `show` surfaces in `role.js` already handle `design-reviewer` identically to `phase-reviewer` and need **no change** (the design and the spot-check both confirm this; only the prompt-note wording in `cmdRun` changes).
- Tests live at **repo-root** `test/`. New consistency assertions extend `test/consistency.test.js` (it already reads files under `plugins/gantry/` and pins literal anchors - the Phase 6 `phase-reviewer` adversary assertions at lines 294-361 are the exact pattern to mirror for Stage 1 / design.md). The prompt-note test extends `test/role.test.js` (spawns `role.js` over a `GANTRY_PROJECT_DIR` mkdtemp dir using the existing helpers).
- The new `SKILL.md` Stage 1 step and the `design.md` step reuse the exact same `role.js resolve design-reviewer` -> `role.js run design-reviewer --adversary -- <reviewed-doc> [rubric]` calls the phase gate already uses; no new script, subcommand, or flag is added.
- `.gantry/models.json` schema is unchanged - the `adversary` field already exists and is already valid on `design-reviewer` (v1.5 resolves it). Only prose describing its behavior changes.

## Phase 1: Adversary prompt note made role-appropriate (`role.js`)
**Status:** built
**Goal:** Branch the one-line adversary prompt note in `cmdRun` on the reviewer role so a `design-reviewer --adversary` run reads in terms of the design doc having already passed the primary design review, while `phase-reviewer` keeps the existing diff wording.
**Files:**
- modify `plugins/gantry/scripts/role.js` (the `promptBody` note at lines 217-222, branched on `role`)
- modify `test/role.test.js`
**Verification:** `node --test` from repo root. Extend `test/role.test.js` (use the existing `mk()` / `writeConfig()` / `runAdversary()` helpers and the `CODEX` backend) with cases: (a) a `design-reviewer --adversary` run does **not** emit the diff-specific wording (assert the invocation's composed prompt / stdout carries the design-doc phrasing and not "diff"); (b) a `phase-reviewer --adversary` run still carries the diff wording unchanged. Mirror the existing `runAdversary` spawn cases (lines 262-299) for shape. The four existing `run --adversary` cases (native-refused, identical-to-primary, and the resolve/show cases) must still pass.
**Exit criteria:** the prompt note is role-branched in `cmdRun`; `design-reviewer` gets design-doc wording, `phase-reviewer` keeps diff wording; no change to argv, dispatch, exit codes, or the descriptor shape; `"use strict"` and the file's block comment intact; new and existing `role.test.js` cases pass; full suite green (174+ pass, 0 fail).
**Blockers:** none. The design fixes the branch condition (`role === "design-reviewer"`, already in scope) and the wording intent; the exact sentence is a one-line wording pass bounded by the design (Unit 3).
**Wired-by:** none (the note is emitted by `cmdRun` itself on every `--adversary` run; no separate caller wires a prompt-string change. The `design-reviewer --adversary` invocation that consumes the design-doc wording is added by the orchestrator prose in Phase 2.)

## Phase 2: Orchestrator + command-surface wiring (`SKILL.md` Stage 1, `design.md`, `run.md`)
**Status:** pending
**Goal:** Wire the configured `design-reviewer` adversary into Stage 1 (run once on the clean reviewed doc, fold findings into Stage 1's existing resolve-and-re-run loop under the same informed-escalation cap as Stage 3, fail-open when the backend is absent), mirror it in the `/gantry:design` standalone command, and add the one-line design-adversary mention to `run.md` item 1.
**Files:**
- modify `plugins/gantry/skills/gantry/SKILL.md` (Stage 1, lines 27-32)
- modify `plugins/gantry/commands/design.md`
- modify `plugins/gantry/commands/run.md` (item 1, line 12)
- modify `test/consistency.test.js`
**Verification:** `node --test` from repo root. Extend `test/consistency.test.js` (mirror the Phase 6 `phase-reviewer` adversary assertions at lines 294-361) with assertions reading `SKILL.md`, `design.md`, and `run.md`: (a) `SKILL.md` Stage 1 carries the adversary step - pin literal anchors `design-reviewer`, `--adversary`, `ADVERSARY`, the both-clean gate condition (`/both\s+clean|both.*clean/i`), the fail-open phrasing (`/fail.open|fail open|non.zero|non-zero/i`), and that with no adversary configured the gate is unchanged; (b) `design.md` carries the same adversary step with `--adversary`, the resolve call, the both-clean gate, and the fail-open rule; (c) `run.md` item 1 now mentions the design adversary (pin `design-reviewer` + `--adversary` near the Design review summary). Pin the anchors so a future edit that drops them turns the test red.
**Exit criteria:** `SKILL.md` Stage 1 runs `role.js resolve design-reviewer` after the primary reads "Ready for phase planning: yes", runs `role.js run design-reviewer --adversary -- <reviewed-doc> [rubric]` when configured, folds findings into the existing resolve-and-re-run loop (re-run the primary after the human's edits, then re-run the adversary; gate opens only when both are clean or the human overrules with a logged reason), caps under the Stage 3 informed-escalation rule (2 cycles then the three options), and fails open when the backend is absent; `design.md` mirrors this proportionate to a command doc; `run.md` item 1 gains the one-line mention; with no adversary configured all three are byte-for-byte today's; no AI-tells in the added prose; new consistency assertions pass; full suite green.
**Blockers:** none that block starting. Phase-internal caution: the precise sentence placement of the new step in Stage 1 (after the primary-clean check, before the Stage 2 gate) is a wording pass bounded by the design's fixed outcomes; if a placement would contradict an existing Stage 1 control-flow step (the resolve-and-re-run loop in steps 2-3, or the "already-approved doc -> skip Stage 1" hard rule), stop and report rather than reordering the existing gate silently.
**Wires:** the `design-reviewer --adversary` invocation added here is the live caller of the role-appropriate prompt note from Phase 1 (and the first stage to invoke the long-resolved `design-reviewer` adversary surfaces in `role.js`). (Wires Phase 1's note; wires the previously-dormant `design-reviewer` adversary path.)

## Phase 3: Drop the "reserved for v2" caveat + flip its pinning test (`models.md`)
**Status:** pending
**Goal:** Change `models.md`'s prose from "a `design-reviewer` adversary is parsed and shown but dormant / reserved for v2" to the active design-gate behavior, and **flip the existing `consistency.test.js` assertion in the same phase** so the suite stays green.
**Files:**
- modify `plugins/gantry/commands/models.md` (line 29)
- modify `test/consistency.test.js` (the existing assertion at lines 402-414)
**Verification:** `node --test` from repo root. In `test/consistency.test.js`, **replace** the existing `"models.md states the design-reviewer adversary is parsed and shown but dormant"` test (lines 402-414, currently asserting `models.md` matches `/dormant|v2|reserved/i`) with its inverse: assert `models.md` still mentions `design-reviewer` AND that it no longer claims the adversary is "dormant" / "reserved for v2" / "does not fire" (assert the negative, e.g. `!/dormant|reserved for v2|does not fire/i.test(text)`), so the dropped caveat cannot silently return. Do not merely add a new assertion alongside the old one - the old positive assertion will fail once the prose changes, so it must be removed in this same edit. Confirm the README-command-table consistency assertion still passes (the `models.md` text change adds/removes no command, so the table is untouched).
**Exit criteria:** `models.md` describes the `design-reviewer` adversary as active in the design gate (runs once on the clean reviewed doc when configured, both verdicts gate Stage 2, fail-open), consistent with the Phase 2 `SKILL.md`/`design.md` prose; the v2/dormant caveat is gone; the flipped consistency assertion passes and guards against the caveat returning; no AI-tells in the added prose; full suite green.
**Blockers:** none. The design names this flip as a required phase task (Unit 4), not a side effect, and fixes both the prose direction and the assertion inversion.
**Wired-by:** none (`models.md` is the `/gantry:models` command doc, read at run time; a prose change needs no separate caller. It surfaces the now-active behavior to the user.)

## Phase 4: Refresh the stale "dormant (v2)" test wording (`role.test.js`)
**Status:** pending
**Goal:** Refresh the comment/description text in the `"show: dormant design-reviewer adversary is parsed and shown"` test so the test name no longer asserts a caveat the feature removes, without changing the test's behavior (which is genuinely unaffected - `show` listing a configured `design-reviewer` adversary is correct before and after this feature).
**Files:**
- modify `test/role.test.js` (the test at lines 235-260, its name string and the inline comment at line 258 mentioning "dormant (v2)")
**Verification:** `node --test` from repo root. The test's assertions are unchanged (it still spawns `show` over a `GANTRY_PROJECT_DIR` mkdtemp dir and asserts the output lists the `design-reviewer` adversary); only the test name and the trailing comment text are reworded to drop "dormant" / "(v2)". Confirm the test still passes under its new name and the full `role.test.js` suite is green.
**Exit criteria:** no test in `role.test.js` names or comments the `design-reviewer` adversary as dormant or v2-deferred; the behavior asserted is identical to before; full suite green (174+ pass, 0 fail).
**Blockers:** none. The design (Unit 4) explicitly states this test's behavior is unaffected and only its description/comment text is stale.
**Wired-by:** none (test wording refresh; no capability added).

## Cross-cutting concerns

- **Orchestrator control-flow contract (`SKILL.md` Stage 1, `design.md`, `run.md`) - Units 1, 2, plus the `run.md` contract note. Phase 2.** What changes: Stage 1 gains a design-adversary step that runs after the primary-clean check and folds findings into the existing resolve-and-re-run loop; `design.md` mirrors it for the standalone command; `run.md` item 1 gains a one-line mention to stay consistent with the skill. What it affects: the live behavior of `/gantry:run` and `/gantry:design`. Ordering: lands in Phase 2, after the Phase 1 prompt-note edit so the wording the design-adversary invocation produces is already role-appropriate. Migration/rollback: additive doc instructions, no data migration; with no `design-reviewer` adversary configured the new step never fires, so the design gate is byte-for-byte today's. Rollback: revert the prose; `role.js` still resolves and shows a `design-reviewer` adversary, it simply goes uninvoked again (back to dormant), exactly the design's stated rollback.

- **`models.md` prose vs. its pinning test (the test-flip contradiction the design review caught) - Unit 4. Phase 3.** What changes: `models.md`'s prose flips from "dormant / reserved for v2" to the active behavior, which makes the **existing** `consistency.test.js:402-414` assertion (matching `/dormant|v2|reserved/i`) fail. What it affects: the test suite goes red if the prose changes without the assertion flip. Ordering: the `models.md` edit and the assertion inversion **must land in the same phase (Phase 3)** - this is the explicit reason this is its own phase rather than being folded into Phase 2's prose work. Migration/rollback: prose-and-test-only; rollback reverts both together (the prose back to the caveat and the assertion back to the positive `/dormant|v2|reserved/i` match).

- **`role.js` adversary prompt note (prose-in-code), branched by reviewer role - Unit 3. Phase 1.** What changes: the one-line note appended for an adversary run is branched on `role`; `design-reviewer` gets design-doc wording, `phase-reviewer` keeps diff wording. What it affects: the prompt text every `--adversary` invocation composes, and the `role.test.js` cases that assert the wording. No change to argv, dispatch, exit codes, or the resolved descriptor shape. Ordering: Phase 1, before Phase 2 wires the live `design-reviewer --adversary` caller. Migration/rollback: revert the branch; both roles fall back to the single diff-worded note (the pre-feature state). The existing `phase-reviewer` adversary diff wording is preserved across the change, so no existing `role.test.js` case regresses.

- **No schema change, no new config, no routing/dispatch change, no `role-core.js` change, no new dependency.** Explicitly out of scope per the design so the implementer does not add one. The `adversary` field already exists and is already valid on `design-reviewer` (v1.5 resolves it); `role-core.js` and the `resolve` / `run --adversary` / `show` mechanics are untouched. The `phase-reviewer` adversary, Stage 3 step 5, and `review.md` are untouched. A `design-reviewer` adversary identical to the primary is already skipped at resolve time (inherited, not re-implemented). This feature is prose plus one branched prompt string plus tests.

- **Default-off invariant.** With no `design-reviewer` adversary configured, every change in this plan is inert: Phase 1's note only fires on an `--adversary` run, Phase 2's Stage 1 step sees `ADVERSARY: none` and skips, Phase 3's `models.md` only documents the opt-in path, and Phase 4 is test wording. The shipped default stays a single native design reviewer; the feature is opt-in per project, never mandatory or default-on (the design's Out of scope).
